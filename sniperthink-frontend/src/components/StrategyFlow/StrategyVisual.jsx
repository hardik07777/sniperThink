import AIAnalysisVisual from "./AIAnalysisVisual"
import AutomationVisual from "./AutomationVisual"

export default function StrategyVisual({ step }) {

  // if (!step) return null

  // if (step.visual === "ai") return <AIAnalysisVisual />
  // if (step.visual === "automation") return <AutomationVisual />

}